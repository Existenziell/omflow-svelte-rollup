<script>
  // We know: isLoggedIn / isTeacher
  // get own user.id from store
  // $teachers are in store, filter your data with userId
  // Show my user data and the connection too ?
  // Features: set my location / create class

  // - teacher info
  // - Create class
  // - ratings
  // - map: what reach do I have
  // - Changes restricted to admins: number of students per class, attributes styles and levels
</script>

<section>Teacher</section>

You have been x days a teacher on Omflow.
<!-- {JSON.stringify(teacher)} -->
